<?php
namespace app\controller;

use think\facade\Request;

class rely
{
    public function get($name=""){
        //dump(Request::has("name","get"));
//        dump(Request::param("name"));
//        dump(Request::param(["name","id"]));
//        dump(input("get.name"));
//        dump(Request::isGet());
//        dump(Request::header());
    }
}